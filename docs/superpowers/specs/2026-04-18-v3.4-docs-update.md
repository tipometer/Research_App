# Docs v3.4 Batch Update — 2026-04-18

Following the C2a sprint (fallback + prompt injection sanitization),
the three source documents at `/Users/balintkovacs/Work/ClaudeCode/Research_App/`
(PRD, UI spec, Handoff) were batch-updated v3.3 → v3.4.

This file is a repo-side marker for git-tracking the out-of-repo doc update.

Key v3.4 additions:
- Runtime fallback layer (isFallbackEligible classifier, executeWithFallback
  one-shot orchestrator, resolvePhaseWithFallback single DB lookup)
- streamStarted flag pattern for Synthesis (pre-stream fallback OK, mid-stream
  err.wasStreaming tag, no restart)
- Prompt injection sanitization (4-zone trust model, 11 injection regex,
  8-token cross-escape strip)
- Admin UI cross-provider warning + two-click confirm
- fallback_used SSE event + audit log
- ResearchProgress phase card badges + aggregated toast

Handoff §16 and §17 added, §15 Decisions log extended to 39 items.

Corresponds to PR: #3 (C2a).
