# Docs v3.3 Batch Update — 2026-04-18

Following the C1 sprint's live integration testing on Vercel AI SDK v6,
the three source documents in `/Users/balintkovacs/Work/ClaudeCode/Research_App/`
(PRD, UI spec, Handoff) were batch-updated from v3.2 to v3.3.

This file is a repo-side marker pointing at the out-of-repo doc updates.
The actual edits are in the parent folder docs.

Key v6 vs. v5 discrepancies captured in v3.3:
- `generateText + output: Output.object(...)` canonical (NOT `generateObject`)
- Gemini Search Grounding via `tools: { google_search: google.tools.googleSearch({}) }`
- Google API: `tools + output` incompatible → grounded phases use manual JSON parse
- Anthropic structured output rejects number `minimum/maximum` → post-parse clamp
- OpenAI strict schema requires `.nullable()` (not `.optional()`)
- Claude default `maxOutputTokens` 128000 fails → explicit `8192`
- Zod v4: `.issues` (not `.errors`)
- Schema relaxation: summary + verdictReason max 1500 chars (was 500)
- Mandatory-citation prompts force `groundingChunks` population
- Type renames: `LanguageModelV1` → `LanguageModel`, `CoreMessage` → `ModelMessage`

C1-fix PR follow-ups also documented in v3.3:
- testProvider 10s per-(user, provider) cooldown
- source-insert error logging with researchId + url context
- Brainstorm.tsx schema adaptation (string ID, no score)
- ResearchReport Survey tab live tRPC (trpc.survey.getByResearch)
